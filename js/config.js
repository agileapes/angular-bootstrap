BootstrapUIConfig = {
    namespace: "ui",
    base: "..",
    debug: true,
    preloadAll: false,
    console: {
        replace: true,
        output: true,
        format: "%y-%m-%d %H:%M:%S.%l [%L] %s",
        preserve: false
    },
    form: {
        preloadAll: true
    }
};