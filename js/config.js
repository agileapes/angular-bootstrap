window.BootstrapUIConfig = {
    namespace: "ui",
    base: "..",
    debug: true,
    preloadAll: true,
    console: {
        replace: false,
        output: true,
        format: "%y-%m-%d %H:%M:%S.%l [%L] %s",
        preserve: false
    },
    form: {
        preloadAll: true
    }
};