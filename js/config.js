BootstrapUIConfig = {
    namespace: "ui",
    base: "..",
    debug: true,
    preloadAll: false,
    console: {
        replace: false,
        output: true,
        format: "%y-%m-%d %H:%M:%S.%l [%L] %s",
        preserve: false
    },
    form: {
        preloadAll: true
    }
};